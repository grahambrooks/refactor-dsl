# Per-repo release config, consumed by .github/workflows/release.yml
# and scripts/render-formula.py. Keep values unquoted (loaded via GITHUB_ENV).
BIN_NAME=refactor
FORMULA_NAME=refactor-dsl
FORMULA_CLASS=RefactorDsl
FORMULA_LICENSE=MIT
FORMULA_DESC=A DSL for multi-language code refactoring with Git-aware matching
